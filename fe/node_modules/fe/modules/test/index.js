const defaults = {
  babel: 'inherit',
  require: ['babel-register']
}

module.exports = async (args, options, logger) => {
  const test = require('ava/lib/cli')
  const { cwd } = args
  // remove sub-command 'fe' from args
  process.argv.splice(2, 1)

  // TODO: inject
  test.run({ cwd, defaults })
}
