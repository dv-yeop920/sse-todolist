const download = require('download-git-repo')
module.exports = (repo, tmp) =>
  new Promise((resolve, reject) =>
    download(repo, tmp, err => {
      if (err) return reject(err)
      resolve()
    })
  )
