const prettyjson = require('prettyjson')
const envCheck = require('./envCheck')

module.exports = async (args, options, logger) => {
  const env = await require('../runtime/')(null, null, options)
  await envCheck(env)
  console.log(prettyjson.render(env))
}
