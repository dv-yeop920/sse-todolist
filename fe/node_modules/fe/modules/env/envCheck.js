// const { exists } = require('../../utils/process')
const { inline } = require('../../utils/console')

// TODO check list:
// dev server is running?
// check node > 8
// check yarn ?
// Parse fe.config.js failed...
// network
// fe.config.js
// ...
// running process

module.exports = async env => {
  console.log('=========================')
  // inline.success(`Your system meet the demand`)
  inline.error('You should install node version 8+')
  console.log(' ', 'Fixes: brew install/upgrade node')
  inline.error(`Can not found fe.config.js`)
  console.log(' ', 'Fixes: cd [project] && touch fe.config.js')
  inline.error(`Can not found your entry file`)
  console.log(' ', 'Fixes: cd [project] && touch index.js')
  inline.warning(`You'd better install yarn intead of npm`)
  console.log(' ', 'Fixes: npm i yarn -g')
  inline.info(`Network is in china`)
  inline.info(`Dev Server is running as: pid`)
  console.log('=========================')
}
