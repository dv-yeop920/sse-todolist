module.exports = (runtime = {}, env = {}) => ({
  swagger: {
    info: {
      title: 'API',
      description: 'APIs for the project.',
      version: '0.1.0'
    },
    host: `${env.SERVER_IP ||
      runtime.project.config.DEV_SERVER_IP}:${env.SERVER_PORT ||
      runtime.project.config.DEV_SERVER_PORT}`,
    schemes: ['http'],
    consumes: ['application/json'],
    produces: ['application/json']
    // securityDefinitions: {
    //   apiKey: {
    //     type: 'apiKey',
    //     name: 'apiKey',
    //     in: 'header'
    //   }
    // }
  },
  exposeRoute: true
})
